# Nel __init__ aggiungi:
self.TRAILING_STOP_PCT = 0.03  # 3% trailing

# Nella classe portfolio position, traccia il massimo:
# Quando compri:
def execute_buy(self, symbol, price, position_size, signal_strength):
    if self.dry_run:
        self.portfolio[symbol] = {
            'entry_price': price,
            'amount': position_size,
            'entry_time': datetime.now(),
            'signal_strength': signal_strength,
            'highest_price': price  # ← NUOVO!
        }
        # ...

# Nel should_sell, aggiungi logica trailing:
def should_sell(self, symbol, rsi, fear_greed, entry_price, current_price):
    # ... codice esistente ...
    
    # Update highest price
    if current_price > self.portfolio[symbol]['highest_price']:
        self.portfolio[symbol]['highest_price'] = current_price
        self.save_portfolio()
    
    highest = self.portfolio[symbol]['highest_price']
    trailing_stop_price = highest * (1 - self.TRAILING_STOP_PCT)
    
    # TRAILING STOP CHECK (prima dello stop loss fisso)
    if current_price <= trailing_stop_price and profit_pct > 0:
        return True, 1.0, f"📉 TRAILING STOP: Peak ${highest:.2f} → ${current_price:.2f} ({profit_pct*100:.2f}%)"
    
    # ... resto del codice ...
